task cucumberTest() {
    doLast {
        def command = "adb shell am instrument -w -e testType cucumber com.fsck.k9.planck.ui.activities.test/com.fsck.k9.planck.ui.activities.test.CucumberTestCase"
        exec {
            commandLine command.split(' ')
        }
    }
}
//hello

task connectedCheckAll(dependsOn: 'connectedEnterprisePlayStoreDebugAndroidTest')
connectedCheckAll.finalizedBy cucumberTest
cucumberTest.dependsOn(installTestDebugApp)
