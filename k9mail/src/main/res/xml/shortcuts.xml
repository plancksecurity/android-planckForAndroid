<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="pEpmail"
        android:enabled="true"
        android:icon="@mipmap/ic_pep_launcher_round"
        android:shortcutShortLabel="@string/pep_mail_shortcut_short_label"
        android:shortcutLongLabel="@string/pep_mail_shortcut_long_label"
        android:shortcutDisabledMessage="@string/pep_mail_shortcut_disabled_message">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="security.pEp.debug"
            android:targetClass="com.fsck.k9.pEp.ui.activities.SplashActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>
